PORT=
DATABASE_URL=
JWT_SECRET=

# SMTP / Email settings (used for email verification codes)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=

# Base URL used to build public links (e.g. certificate download links in emails)
BASE_URL=http://localhost:3000

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Redis queue config
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
FRONTEND_URL=http://localhost:5173

# Rate limiting (window in milliseconds, max = requests per window per IP)
RATE_LIMIT_GLOBAL_WINDOW_MS=60000
RATE_LIMIT_GLOBAL_MAX=200
RATE_LIMIT_API_WINDOW_MS=60000
RATE_LIMIT_API_MAX=120
RATE_LIMIT_AUTH_WINDOW_MS=900000
RATE_LIMIT_AUTH_MAX=20
RATE_LIMIT_LOGIN_WINDOW_MS=900000
RATE_LIMIT_LOGIN_MAX=5
RATE_LIMIT_PASSWORD_RESET_WINDOW_MS=900000
RATE_LIMIT_PASSWORD_RESET_MAX=5
