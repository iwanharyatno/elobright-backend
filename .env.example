PORT=
DATABASE_URL=
JWT_SECRET=

# SMTP / Email settings (used for email verification codes)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=

# Base URL used to build public links (e.g. certificate download links in emails)
BASE_URL=http://localhost:3000

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Redis queue config
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
FRONTEND_URL=http://localhost:5173


# Rate limiting: one shared per-IP bucket for all /api routes (counters stored in Redis)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=200
